2)	Repo migration from ADO to github using github enterprose importer:
On poweshell(admin)
winget install --id GitHub.cli
Reopen the powershell
gh –version
login to Github To login: 
gh auth login
 Select Github.com > HTTPS > Yes > Paste github authentication token.

 

Install ADO2GH plugin:
gh extension install github/gh-ado2gh
    
Update the plugin:
 gh extension upgrade github/gh-ado2gh

Set git&Pat token in env:
$env:GH_PAT=”TOKEN” 
$env:ADO_PAT=”TOKEN” 


To migrate one repository:
gh ado2gh migrate-repo --ado-org SOURCE --ado-team-project TEAM-PROJECT --ado-repo CURRENT-NAME --github-org DESTINATION --github-repo NEW-NAME
example:

gh ado2gh migrate-repo --ado-org equitable-us --ado-team-project IT --ado-repo eb-services --github-org  EQU-IT --github-repo eb-services

